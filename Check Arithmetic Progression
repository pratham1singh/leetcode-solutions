class Solution:
    
    def checkIsAP(self, arr, n):
        arr.sort()
        diff=arr[1]-arr[0]
        l=len(arr)
       
        for i in range(1,l-1):
            if diff==(arr[i+1]-arr[i]):
                continue
            else:
                
                return False
        return True
